# Precursors
a tif file is necessary that is in little endian and in wich the image data is stored in a singular data strip
>[!important]
>this script requires 2x the memory of the size of the image file itself.